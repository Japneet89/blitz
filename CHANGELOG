CHANGELOG

1.0:
- modified backend to use gstore-node for schema definition and validation
- modify backend to use gstore-api for automatic route generation
- backend: each entity has an ancestor with a group entity so data can be separated
- backend: modified python to be a local script run via child_process. still needs fixes
- frontend: removed webpack due to hot reload issues and made part of create-react-app
- reorganized frontend to be in containers, components, modals, and entities
- reorganized frontend to have more unification in naming across the same entities and actions
- removed all hard coded user ids etc. from frontend
- added ability to view/delete/edit/add drawers and containers
- added search modal for integration with ml recommendations
- converted backend to serve both SPA and data and ML recommendations